---
apiVersion: v1
kind: Service

metadata:
  name: configmap-nginx

spec:
  type: NodePort

  selector:
    app: configmap-nginx

  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
      nodePort: 31554